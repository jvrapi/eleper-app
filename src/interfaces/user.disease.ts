export interface Save {
  userId: string;
  diseaseId: string;
  active?: boolean;
  diagnosisDate?: Date;
}

export interface UserDisease {
  userId: string;
  diseaseId: string;
  active?: boolean;
  diagnosisDate?: Date;
}
