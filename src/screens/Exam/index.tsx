import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const Exam = () => {
  return (
    <View style={styles.container}>
      <Text>Exam screnn</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});

export default Exam;
